listen_addr = ":9000"

table_file_path = "db/table"

# The amount of time between saves to disk.
save_duration = "10s"

numeric_columns = [
  ["clicks", "uint16"],
  ["age", "uint8"]
]

string_columns = [
  ["name", "uint16"],
  ["country", "uint8"]
]
